# Voyage 개발 로드맵

> 계획 기준: 주 5일 · 하루 ~5 집중시간 (가용 8시간 중 슬랙 반영) ≈ **약 4 작업일/주**
> 아트는 전부 플레이스홀더 전제. 진짜 아트·애니메이션·폴리시는 별도.
> 이 문서는 살아있는 계획 — 막히면 뒤로 밀고 버퍼 주가 흡수한다.

## 시계 탭 구조 (설계)

```
시계 탭
├── 세션 (포모도로)   ← Pomodoro(Countdown)   : 집중/휴식 단계별 카운트다운
│     설정: 집중 시간, 짧은 휴식, 긴 휴식, 반복 사이클 수
├── 타이머            ← Countdown 직접          : 정해진 시간 뒤 알람 (1회)
├── 알람              ← 별도 부품 AlarmClock     : 목표 "벽시계 시각" 도달 감지
│     (요일/반복 로직은 데일리 체크리스트와 재사용)
└── 설정              ← 지속 전역 설정 (Autoload, 단일 진실)
	  ├ 카운트다운 보이기/숨기기   (뷰)            — 즉시 가능
	  ├ 타이머 스타일 Timeline/Circle (뷰 교체)    — 즉시 가능
	  ├ 소리 설정                 (오디오)         — 오디오 노드 필요
	  └ 활성화 시 자동 최소화      (컴패니언 동작)   — Week 7 의존
```

설계 원칙:
- **세션·타이머는 Countdown 부품 공유. 알람은 별도 메커니즘** (지속시간 카운트다운이 아니라 시각 감지).
- **타이머 스타일은 뷰 문제** — 로직은 `ticked` 만 쏘고 표시 위젯(Timeline/Circle)을 갈아끼움. 스타일 분기를 타이머 로직에 넣지 않는다.
- **설정값은 전부 데이터 계층(Autoload)에 모은다** — 시계 뷰와 컴패니언이 같은 곳을 읽는다.
- **자동 최소화**는 설정 항목만 먼저 정의, 실제 동작은 컴패니언(Week 7) 연결.
- 빌드 순서: **세션 → 데이터 계층 → 설정/알람** (설정은 저장할 곳이 있어야 의미 있음).

## MVP — 7주

### Week 1 · 타이머 완성 + 데이터 계층 착수 — ✅ 완료
- [x] 포모도로 (유한 세션 계획 · skip · 타임라인 칩 · 인라인 설정 · save-on-start)
- [x] 일반 타이머 (시/분/초 입력 · 영속화)
- [x] Autoload `Save` + 저장/불러오기 (JSON, `user://save.json`, version 필드)
- ➕ 계획 초과: 알람 탭(CRUD·발화·소리), 설정 탭, ClockTab 커스텀 nav, `ClockToolView` 공통 베이스
- ⚠️ 목표의 "총 집중시간 persist" 는 미완 — `focus_finished` 가 아직 누적 안 함 → **Week 4(항해 진행)** 소관 (영속화 인프라는 준비됨)

### Week 2 · 데이터 계층 마무리 + Todo 기초
- [x] 자동저장, 시작 시 로드, 세이브 스키마 정리
- [x] Todo: 추가/체크/삭제 + 영속화
- ✅ 목표: 할 일이 재시작 후에도 유지됨

### Week 3 · Todo 완성 + 데일리 체크리스트  ⚠️ 위험 주
- [x] 드래그 정렬, 카테고리 분류 — 재사용 DragHandle/ReorderList(commonui)를 태스크·그룹·알람에 적용 + 호버·스크롤
- [x] 데일리: 습관 트래커(주간 그리드·요일 활성화·달성도 원형·주간 페이지네이션·백필·randi id). 자정 리셋=주 단위 롤오버(뷰 열림 시 지연 감지)
- ✅ 목표: 작업 관리 기능 완성

### Week 4 · 항해 진행 + 발견 + 앱 셸 — **상주 월드 셸 확정**
> 앱 셸 = **상주 월드 셸**(월드가 바탕, 도구는 그 위 팝업). 상세·결정 근거는 architecture.md "앱 셸 — 상주 월드 셸".
> 좌측 세로 도크 / 패널 한 번에 하나(hide 토글, 타이머 보존) / 돌아가는 타이머는 월드에 상시 HUD.
- [x] **T1 타이밍 프리미티브** (완료): `Timers`(autoload `TimerManager`) + `TimerHandle`. 클럭 기반 핸들(`set_timer`→`remaining/pause/resume/cancel`). autoload 등록 완료, 정적 검증 통과.
- [x] **T2 세션 소유자** (완료): T2a(타이밍 재배선)/T2b(소유 이전)로 분할 진행.
  - T2a: `pomodoro.gd` 핸들 기반 재작성 ✅, `TimerManager.is_paused`+`TimerHandle.is_paused` 추가 ✅, `pomodoro_view.gd` 폴링 `_process` 추가 ✅. **남은 것 = `pomodoro_view.gd`에서 ① 19행 `pomodoro.ticked.connect(_on_ticked)` 삭제, ② `_on_ticked` 함수 삭제** (ticked 시그널 제거됨 → 안 지우면 실행 에러) → 이후 F6(시작/일시정지/스킵/세그먼트전환/세션완료 + 최소화 정확) 검증.
  - T2b: `Clock`(autoload) 도입해 `Pomodoro` 소유, `PomodoroView`를 `$Pomodoro`→`Clock.pomodoro` 바인딩(뷰는 reset 아닌 **sync**), `PomodoroView.tscn`의 `$Pomodoro` 노드 제거.
- [x] **T3 타이머 뷰** (완료): `SimpleTimer`→`Timers` 재배선, TimerView 바인딩, `countdown.gd` 삭제 완료. (알람은 휘발성 아님 → TimerManager 비귀속, "알람 전역화"로 별도) ※ 점검에서 `simple_timer.start()` 가드 `and`→`or` 버그 1건 발견·수정.
- [x] ⓪ 데이터: `Voyage{total_play_seconds, total_focus_seconds, voyage_distance}` + version 4. (플레이=Save 클럭 기반, 종료 저장)
- [x] ① 월드 루트(`World.tscn`, `GameTitle` 교체) → 이후 Ocean_8 픽셀아트 패럴랙스로 발전.
- [x] ② 도크 + 패널 호스트: `ButtonGroupNav`(commonui, 도크·시계서브탭 공용)로 좌측 도크가 뷰를 팝업 open/close(1개 한정, 활성 버튼 재클릭=닫기 `allow_close`).
- [x] ③ 집중 누적: `Clock`이 `focus_finished`/타이머완료 → `Save.voyage.add_focus`(=`total_focus_seconds` 집중 통계). ※ 항해 진행은 ⑤에서 별도 `voyage_distance`로 분리.
- [x] ④ 상시 타이머 HUD: 월드 우상단 카드, active 세션(포모 우선→타이머) 폴링 + 단계 타임라인 + 아이콘 제어(정지/스킵/리셋). 패널↔HUD 동기화 = `running_changed` 이벤트.
- [x] ⑤ 배 반응 + 항해 거리 — **모델 재설계**(상세 architecture "Week 4 구현 결과"):
  - 항해 = **집중 중에만 실시간** 전진(`Clock.is_focusing`), 정박 시 정지. (사용시간 기준 폐기 — 내러티브 + 집중 유도)
  - `voyage_distance`(영속·별도) = 배 속도 적분(감성용, 부정확 OK), `total_focus_seconds`(통계)와 분리. 배지 "leagues".
  - 배 고정 + 사인 bob/rock. 패럴랙스 = Ocean_8 5레이어, 레이어별 `motion_offset=-fmod(거리×scale, mirror)`(루프+float32 정밀).
- [x] (셸 폴리시): 패널 배경 = `TabContainer.panel` 스타일(시계)/`PanelContainer`(todo·habit). 서브탭 흔들림 = `PanelHost`→`Control` + 패널 좌상단 앵커+grow End.
- [x] (고려 처리): 타이머도 완주 시 자동 대기(포모와 일관) / 완료음 `Clock`으로 이동(뷰 의존 제거).
- [x] **⑥ 발견 = 병 속 편지 받기 루프** (완료, F6 2026-06-19): 집중 항해 중 병 앰비언트 발견(비방해 토스트) → 항구 선반 오브젝트(슬라이드-인 + "받은 편지 N" 배지) 클릭 → 양피지 열람(토큰 템플릿+슬롯, 읽는 이 언어로 렌더) → 보관(`Save.letters`=`LetterArchive`, 안정 id) → 선반에서 다시 보기(영속). 시드 풀(큐레이션)만 = MVP-safe. 구현: 범용 `Discovery` + `Mailbox` + `Shelf`(다이제틱) + `LetterView` + `ShelfView` + `LetterContent`. 컴포저·어휘해금=W5/W6, 진짜 교환=Phase 3. 상세 = architecture "발견 콘텐츠 — 병 속 편지".
- [x] (병행) 알람 전역화 (완료, 2026-06-19): 슬라이스 1(autoload `Alarms` 이전 + 뷰 CRUD화 → 패널 닫아도 발화) / 2(월드 배너 공용 `Notice`, 알람=상단·편지=하단 각자 인스턴스) / 3(갭 catch-up·fire-late: "마지막 확인~지금" 벽시계 차이로 놓친 분 발화, `MAX_CATCHUP_MINUTES` 초과 갭=묵은 알람으로 무시). `AlarmClock`=감지·발화 / 뷰=CRUD만.
- ✅ 목표: 집중하면 항해가 (실시간) 나아가고, 도구를 월드 위에서 열고닫음. (발견은 ⑥)

### Week 5 · 유저 기록 열람 + 섬 도감 + 어휘 해금(자체 발견)
- [ ] 섬 도감 (발견 섬 표시) — 병 선반도 같은 도감 패턴으로 합류
- [ ] 섬 발견 = 테마 어휘 해금 → `lexicon` 도입 (편지 컴포저 재료)
- [ ] 일일 활동 기록 열람, TODO 완료, Habit 완료, 포모도로, 타이머 등의 완료 기록이 저장됨
- [ ] 유저 기록 도크를 통해 전체 기록(해리, 플레이 타임 등), 일일 활동 기록, 자유 일지 작성 기능 추가
- ✅ 목표: 발견이 도감에 반영, 편지 어휘가 풀리기 시작, 유저 기록이 수집되기 시작

### Week 6 · 항해 일지(= 편지 작성) + 통합/세이브 안정성
- [ ] 세션 종료 회고 = 템플릿+슬롯 컴포저로 "나도 띄우기"(`sent_letters` 로컬) + 기록 목록
- [ ] 어휘 해금 나머지 소스: 집중 마일스톤 + 받은 편지에서 확률적 구절 보관
- [ ] 전체 흐름 통합, 저장/불러오기 견고화, 엣지 케이스
- ✅ 목표: 하루치 루프가 기록·복원되고, 회고가 편지로 바다에 나감

### Week 7 · 데스크톱 컴패니언 + 마감 버퍼  ⚠️ 위험 주
- [ ] 미니/풀 모드 전환, 투명, always-on-top, 드래그
- [ ] 폴리시·버그 정리 (이 주가 곧 버퍼)
- ✅ 목표: **MVP 기능 완성**

## Phase 2 — 3주 (Week 8~10)

### Week 8
- [ ] 도감 확장 (선원/별자리/해양생물, 섬 도감 패턴 재사용)
- [ ] 관측 모듈 해금 시스템 착수

### Week 9
- [ ] 관측 모듈 해금 마무리
- [ ] 배 외형 성장 (뗏목 → 소형범선 → 탐험선 → 대형범선)
- [ ] 마일스톤 시스템

### Week 10
- [ ] 주간 / 월간 리캡
- [ ] 휴식 페이즈 연출 (다른 배 도크 인/아웃)
- [ ] Phase 2 버퍼

## Phase 3 — 별도 (대략 1~1.5개월, 오차 큼)

서버·동시성은 사실상 **백엔드 프로젝트**다. Godot 작업이 거의 아님.

- 서버 / DB / API
- 익명 처리, 병 속 편지 **진짜 교환**(실송수신 · 좋아요=비동기 상호성 · 신고/차단 · 모더레이션 파이프라인 · 크로스랭귀지 라이브)
- 다른 항해자 흔적, 간접 동접, 항로 흔적
- 호스팅 + **상시 운영 비용**

→ MVP·Phase 2 완료 후 별도로 의사결정.

---

## 메모

- 슬랙 포함 추정. 마지막 20%(버그·연결·세이브 안정성·전환 처리)는 항상 늘어남.
- 가장 위험한 주: **Week 3(드래그·날짜 로직)**, **Week 7(윈도우 다루기)**.
- 데이터 계층(Week 1~2)은 한 번 깔면 이후 모든 기능을 빠르게 함 — 투자 우선순위 높음.

### 미룬 항목 (계획엔 있으나 후순위)
- **타이머 스타일 Timeline/Circle**: 현재 Timeline(바)만. Circle 위젯은 폴리시/별도 작업.
- **활성 시 자동 최소화**: 설정·플래그·print 훅까지 완료. 실제 미니 모드 전환은 **Week 7(컴패니언)**.
- **총 집중시간 추적·영속화**: **Week 4(항해 진행)** 에서 `focus_finished` 연결 시.
- **알람 전역화**: 현재 `AlarmClock` 이 알람 뷰 소유. 앱 셸 생기면 autoload 로 올려 전역 발화 고려.
- **공통 리스트 베이스 추출**: 드래그/재정렬·포커스해제는 `commonui`로 추출 완료(태스크·그룹·알람·습관 4사례). 추가/삭제·진행도 등 CRUD 베이스는 미추출 — 추후.
- **데일리 라이브 롤오버**: 앱 켜둔 채 주 경계 넘김 시 즉시 갱신 — 현재는 뷰 열림 시 검사. Week 4 앱 셸의 화면 열림 훅에 연결.
- **여가 위젯 열린 채 집중 자동시작 처리**(보류, 2026-06-19): 휴식 종료→집중 자동시작 시 편지/선반 등 여가 UI가 열려 있으면 어떻게 처리할지. 후보 = 집중 진입 시 여가 UI 자동 닫기 + 읽던 편지 트레이 복원(`Clock.focusing_changed` 신호 신설), 또는 휴식→집중 자동시작 자체를 막기. **재논의 필요.**
- **Week 4 잔여 정리**(블로커 아님): `Discovery.interval` 테스트값→실제 발견 빈도 튜닝 / 죽은 코드 삭제(`alarm_view._on_alarm_triggered`) · `mailbox._process` 미사용 인자 `_delta`.
- **세이브 파일 세분화(여러 파일)**: 현재 `user://save.json` 단일 파일에 settings·alarms·todo·habit·voyage·letters가 다 들어감 — 점점 비대. 도메인별 분리 파일(예: `settings.json`/`letters.json`/…) 또는 디렉터리로 쪼갤 것. 부분 저장·손상 격리·동시성에도 유리. **지금은 안 함**, 데이터가 더 커지면 착수. 단일 진실(`Save`)·`version`·`to_dict`/`from_dict` 구조는 그대로 유지하고 파일 I/O만 분할.
