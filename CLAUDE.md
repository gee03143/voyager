# Voyage 프로젝트 컨텍스트

## 프로젝트
- **Voyage**: Godot 4.6 self-care 저널 컴패니언 앱 (할일/습관/뽀모도로/저널 + 컴패니언)
- 경로 표기: 모든 경로는 **워크스페이스(저장소) 루트 기준 상대 경로**. 로컬 절대 경로는 적지 않는다(환경마다 다름)
- 사용자: Unreal/C++ 경험 많음, Godot 입문

## 문서 체계
⚠️ **관할이 갈린다 — 같은 사실을 두 문서에 적지 않는다.**

| 문서 | 관할 |
|---|---|
| `docs/README.md` | 제품 약속·의도·방향성 |
| `docs/companion-persona.md` | 컴패니언(헤이즐) 규격. **대사·반응을 건드릴 땐 반드시 읽을 것** |
| `docs/specs/` | 합의된 **기능**이 어떻게 동작해야 하는가 (구현 후에도 보존되는 정본) |
| `docs/architecture/` | 여러 기능이 공유하는 **구조** (인프라·공통 컴포넌트·i18n) |
| `docs/adr/` | 되돌리기 어려운 **결정의 근거** |
| `docs/unreal-godot-mapping.md` | Unreal 감각 → Godot 대응 |
| `docs/implementation.md` | ⚠️ **동결된 유산.** 읽기 전용 참조, 새 내용 추가 금지 |

- **계획·일정·우선순위·백로그는 사용자 관할** — Claude는 작성하지 않음(`roadmap.md` 폐지, 2026-08-10). spec은 계획이 아니라 *산출물 정의*라 이 금지에 해당하지 않는다
- ⚠️ 구버전 문서(상주 월드 셸·병 속 편지 시절)는 정본 아님 — 과거 결정 근거 확인이 필요할 때만 참고
- ⚠️ README와 코드가 어긋나는 지점: (1) 월드(패럴랙스·배·항해 거리) 코드가 `World.tscn`/`world.gd`에 남아 있음 — 진입점은 `MainShell.tscn`로 전환돼 실행되진 않으나 삭제는 보류, (2) always-on-top 컴패니언 모드는 README상 "보류"지만 실제로는 구현되어 동작 중

### spec 파일 (`docs/specs/<기능>.md`)
```md
---
status: planned | implemented | deprecated
---
# 기능 이름

## 동작
- 사용자 관점에서 무엇이 어떻게 동작하는가

## 범위 아님
- 이번에 안 하기로 한 것 (**비워두지 않는다**)

## 메커니즘 영향
- 스키마/시그널/autoload 변경. 없으면 "없음"

## 관련 파일
- `scripts/...`
```
- `planned` → `implemented` 전환은 **사용자가 F6로 동작 확인을 준 뒤에만**
- 폐기 시 `docs/specs/deprecated/`로 이동 (살아있는 spec만 평면에 둔다)

### architecture 파일 (`docs/architecture/<컴포넌트>.md`)
`# 이름` + 파일 경로 → `## 역할` / `## 계약` / `## 주의`. 상태 레이어 없음.

### 문서 서술 규칙 (전 문서 공통)
- **한 불릿 = 한 사실.** 화살표로 여러 단계를 한 줄에 잇지 않는다
- 설명이 3줄을 넘으면 하위 불릿으로 쪼갠다
- 파일 하나 = 기능 하나 / 컴포넌트 하나

### ADR
세 조건이 **전부** 참일 때만: 되돌리기 비쌈 + 근거 없이 보면 이상함 + 진짜 트레이드오프가 있었음.
Claude가 "이건 ADR감입니다"라고 **제안**하고, 승인받으면 작성한다. 한 문단이면 충분하다.

## 작업 규칙 (반드시 지킬 것)
1. **코드 직접 편집 허용.** `.gd`는 자유. `.tscn`은 **조건부** — 편집 전 "이 씬 에디터에서 닫아주세요" 확인, 편집 후 사용자가 Godot에서 열어 확인. (씬을 열어둔 상태의 외부 변경이 어떻게 처리되는지는 확인 안 됨)
2. **git 쓰기 금지.** `commit`/`add`/`push`/`reset`/`checkout`/`stash`를 쓰지 않는다 — 커밋은 사용자의 **최종 리뷰 게이트**다. 읽기(`status`/`diff`/`log`)는 자유. 이 금지를 푸는 것(`.claude/settings.json`의 `deny` 완화)은 사용자만 한다
3. **작업 단위 = 빌드·동작 가능한 슬라이스.** 슬라이스 끝에서 멈추고 **변경 파일 목록 + 무엇을 왜 + F6에서 확인할 동작**을 보고한다. 다음 슬라이스를 앞 슬라이스 위에 쌓지 않는다(사용자가 그 자리에서 되돌릴 수 있게)
4. **착수 게이트는 위험도로 차등**:
   - **바로 착수**: 뷰/글루, 버그 픽스, 문구·레이아웃, 기존 패턴 반복
   - **합의 후 착수**: `Save` 스키마, 저장 파일 포맷, 시그널 배선 구조, 데이터 모델, ID 라이프사이클, autoload 추가/제거, 신규 화면·신규 시스템
   - **spec이 있으면 그 범위는 전면 사전 승인** — `## 메커니즘 영향`에 적힌 것까지 추가 합의 없이 진행. **spec에 없는 메커니즘 변경이 필요해지면 멈추고 spec부터 고친다**(스코프 크립 신호)
   - 컴패니언 대사·반응: 초안을 쓰되 커밋 전 승인
5. **코드 질문/디버그는 먼저 코드 읽기** — 추측 답변 금지, 근거(핀 연결/노드 정의 등) 기반으로만
6. **검토 요청 시 항상 현재 파일을 다시 읽고** 판단 (이전 grep/read 결과 캐시 사용 금지)
7. **엔진·OS 동작 추론 금지** — Godot 엔진이나 OS 동작에 대한 판단은 실제 코드, 엔진 공식 문서, 또는 엔진 소스에 근거해야 한다. 불확실하면 "확인 필요"로 명시
8. **버그 원인 추론 시 의심 코드 명시** — 파일명·라인을 근거로 제시. 수정 시 회귀 가능성을 먼저 검토하고 명시
9. **슬라이싱 ≠ 범위 축소** (중요): "가장 작은 슬라이스부터"는 *빌드 순서*일 뿐, 그 기능을 안 한다는 뜻이 아니다
   - **spec이 곧 산출물 정의**이고 완료 판정 기준이다
   - **"완료"는 기능이 실제 동작(F6 검증)할 때만** 쓴다. 범위를 깎은 뒤 그 기준으로 완료 판정 = 금지
   - 선택지를 "최소/권장"으로 유도해 산출물을 줄이지 말 것
10. **편집 후 문법 체크**: `bash tools/gdcheck.sh <편집한 .gd 파일들>`. 잡는 건 **문법 오류뿐** — 없는 메서드 호출 등은 실행해야 나온다. 동작 검증은 F6(사용자)
11. **작업 단위 종료 시 문서 최신화**:
    - 새 사실은 `docs/specs/` 또는 `docs/architecture/`에 쓴다
    - `docs/implementation.md`는 **동결** — 갱신이 필요해진 절은 **갱신하지 말고 이관 후 원본에서 삭제**한다
12. 한국어 존댓말, 간결. 표/체크리스트 남발 금지(채팅 응답 기준 — 문서에는 적용 안 함)
13. **설계 질문은 한 번에 하나씩.** 여러 개를 한 라운드에 몰아 물으면 사용자가 각각을 제대로 판단할 수 없다. 질문 하나 = 선택지 + 추천 + 근거. 답을 받은 뒤 다시 계산해 다음 하나를 묻는다. 남은 질문은 한 줄로 예고만 한다

## 아키텍처 원칙 (확립됨)
- **메커니즘 vs 뷰/컨트롤러 분리**: 메커니즘(`Pomodoro`/`SimpleTimer`/`AlarmClock`, 지속시간 타이밍은 `Timers`)은 `Save`·전역을 **모름**(재사용·테스트 가능). 뷰/컨트롤러가 `Save`를 알고 배선한다
- **공통 뷰 동작은 베이스로 추출**(`ClockToolView`). 원칙: "하나만 보고 일반화 안 함 → **두 번째 사례에서** 추출"
- **저장 정책**: 세션 duration = save-on-start / 전역 설정 = save-on-change(`AppSettings.changed`→`Save`) / 알람·Todo·습관 = 디바운스 저장(0.5s)
- 결합도: 알림은 **signal**, 단일 진실은 **`Save`**(autoload) — 저장 파일은 `save.json`/`records.json`/`journal.json`/`todo.json`/`gratitude.json`/`mood.json` **6개**로 분리
- 다국어: 원본 문자열=키, 문장 통째로 포맷(조각 연결 금지)
- 미래 서버/sync 대비: 단일 진실(`Save`)·JSON·`version` 필드
  **안정 ID**: Todo·알람은 스냅샷(ID 없음) 유지. 습관·저널·기록·편지는 ID 도입(`IdGen`, randi 정수). 도입 기준 = 항목을 정체성으로 다뤄야 할 때(반복·참조·sync)
